module Test where
    -- dMe1 :: Int -> Int
    -- dMe1 x = x * x

    -- dMe2 :: Float -> Float
    -- dMe2 y = y * y

    -- dMe3 :: Num a => a -> a
    -- dMe3 z = z * z

    -- dMe4 :: Num a => a -> a
    -- dMe4 x = x * x

    if x<0 then
        x=x*(-1)
        else
            